# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** นายธีรนัย ไชยกันทะ **รหัส:** 68543210066-5 **Sec:** 2 **ชุด:** B

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## ตัวอย่าง (ดูระดับรายละเอียดที่ต้องการ — ลบทิ้งได้ หรือปล่อยไว้ก็ได้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 10:05 | B2 | "React กรอง array ด้วยหลายเงื่อนไขพร้อมกันยังไง" | แนะนำให้ใช้ `.filter()` ต่อกันสองชั้น | ใช้แนวนี้ แต่แก้ให้ค้นทั้ง `requesterName` และ `details` + ใส่ `toLowerCase()` · ทดสอบพิมพ์ "ห้อง" ได้ REQ-002, REQ-003 ตรงโจทย์ |
| 10:38 | B3 | "กดปุ่มแล้ว badge เปลี่ยน แต่แผงสรุปไม่อัปเดต เพราะอะไร" | บอกว่าต้อง `setState` ด้วยข้อมูลชุดใหม่ ไม่ใช่แก้ของเดิม | เข้าใจว่าต้อง `setRequests(nextRequests)` ที่ได้กลับจาก `updateRequestStatus` · ลองรีเฟรช (F5) แล้วค่ายังอยู่ = persist จริง |
| 10:52 | B4 | "เขียน component ให้แสดงป้ายตามค่าที่ส่งมา" | ให้โครง if-return 2 กรณี | เพิ่มกรณีที่ 3 (ค่าอื่น → "ไม่ระบุ") เองเพราะข้อมูลอาจมีค่าที่ไม่คาดคิด · ทดสอบด้วย `priority="high"` ได้ "ไม่ระบุ" |
| 10:15 | B1 | `ไม่ได้ใช้ AI` (แก้เองจากการอ่าน Warning ใน Console) | — | — |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 14:05 | B1.1 | "ทำไมขึ้นเตือน Warning: Each child in a list should have a unique 'key' prop ที่ SummaryPanel" | อธิบายเรื่อง Reconciliation และแนะนำให้เพิ่ม key prop ให้กับ Root Element ใน .map() | เพิ่ม key={stat.type} ที่ <article> ใน SummaryPanel.jsx · รัน npm run dev ตรวจ Console ไม่พบ Warning สีเหลืองแล้ว |
| 14:12 | B1.2 | "ตัวเลข completed ใน SummaryPanel ไม่ตรงกับจำนวนการ์ดจริง แก้ตรงไหน" | ชี้จุดใน useMemo ว่าเงื่อนไข .filter() สะกดชื่อสถานะไม่ตรงกับ Schema Data | เปลี่ยนเงื่อนไขเป็น request.status === 'completed' ใน DashboardPage.jsx : 50 · ตรวจสอบบนหน้าจอแสดงผลเสร็จสิ้นเป็น 2 ตรงตามโจทย์ |
| 14:20 | B1.3 | "กดปุ่ม FilterBar เปลี่ยนสถานะแล้วรายการการ์ดไม่กรองตาม" | บอกว่าใน filteredRequests มีการ Hardcode ค่า 'pending' ไว้ | แก้ไข DashboardPage.jsx : 55 ให้เปรียบเทียบกับ state statusFilter · ลองกดปุ่มตัวกรองทุกสถานะ พบว่าการ์ดเปลี่ยนตามถูกต้อง |
| 14:30 | B1.4 | "เปลี่ยน URL Parameter เช่น REQ-101 เป็น REQ-102 แล้วข้อมูลหน้า Detail ไม่ยอมอัปเดต" | อธิบายเรื่อง Effect Lifecycle ว่าเกิดจาก Dependency Array ของ useEffect เป็น array ว่าง | เติม [requestId, reloadKey] ใน RequestDetailPage.jsx : 28 · ลองเปลี่ยน URL สลับ ID แล้วหน้าจอโหลดข้อมูลใหม่ทันที |
| 14:40 | B1.5 | "กดลบการ์ดแล้วการ์ดหาย แต่ตัวเลขใน SummaryPanel ไม่ลดลงตาม" | อธิบายว่าตัวเลขคำนวณจาก state หลัก requests แต่ฟังก์ชันลบไม่ได้ไปอัปเดต state นั้น | สั่ง setRequests(nextRequests) ใน handleDelete (DashboardPage.jsx : 65) · ทดสอบลบการ์ดแล้วตัวเลขแผงสรุปลดลงทันที |
| 14:50 | B1.6 | "กดปุ่มลบการ์ดแล้วหน้าพังหรือเปลี่ยนหน้าเอง เกิดจากสาเหตุอะไร" | อธิบายเรื่อง Event Bubbling จากปุ่มลบทะลุไปยัง <Link> นอกสุด และแนะนำใส่ e.stopPropagation() | เขียนฟังก์ชัน handleDelete ใน RequestCard.jsx : 16 เรียก e.stopPropagation() ก่อนลบ · ทดสอบกดลบแล้วการ์ดหาย หน้าไม่พัง |
| 15:10 | B2.1 | "อยากสร้างช่องค้นหา input ใน React ต้องสร้าง useState ยังไง แล้วเอา input ไปวางตรงไหนของการ์ด" | แนะนำให้สร้าง useState('') ไว้เก็บคำค้นหา และเอา <input> วางไว้ข้างบน <RequestList /> | เขียน const [searchText, setSearchText] = useState('') แล้ววางช่อง input เหนือรายการการ์ดใน DashboardPage.jsx · ทดสอบพิมพ์แล้วตัวอักษรขึ้นปกติ |
| 15:20 | B2.2 | "เขียน filter ค้นหาพิมพ์เล็กพิมพ์ใหญ่ให้เจอทั้งประเภทและสถานที่ยังไง ลองเขียนแล้วมันขึ้น error" | ให้ใช้ .toLowerCase() และ .includes() พร้อมใส่ตัวเช็คป้องกันค่าว่างเปล่าพัง | เอาโค้ด filter มาแก้ตามใน DashboardPage.jsx · ลองพิมพ์คำว่า "ซ่อม" แล้วขึ้นการ์ด REQ-101 กับ REQ-104 ถูกต้อง |
| 15:30 | B2.3 | "ถ้าจะกดปุ่มกรองสถานะ พร้อมกับพิมพ์ค้นหาในช่อง input ไปด้วย ต้องเขียนเงื่อนไข filter ต่อกันแบบไหนดี" | แนะนำให้ใช้ && เชื่อมเงื่อนไขการกรองสถานะและการกรองข้อความค้นหาเข้าด้วยกัน | ปรับเงื่อนไขใน filteredRequests ให้เช็คทั้งสองอย่างพร้อมกัน · ลองเลือกปุ่ม "เสร็จสิ้น" แล้วพิมพ์ "ซ่อม" ได้ REQ-104 การ์ดเดียวตรงตามโจทย์ |
| 15:40 | B2.4 | "ค้นหาแล้วไม่เจออะไรเลย อยากให้มันขึ้นข้อความว่าหาไม่เจอ ต้องเขียน if else ใน React ยังไง แล้วแผงข้างบนจะพังไหม" | แนะนำใช้ Ternary (? :) เช็คความยาวของอาร์เรย์ว่าเท่ากับ 0 ไหม และอธิบายว่าแผงสรุปจะไม่พังเพราะใช้ตัวแปรคนละตัวกัน | เขียนเงื่อนไขเช็คความยาวอาร์เรย์ ถ้าเป็น 0 ให้แสดงข้อความเตือน · พิมพ์ "หกรเอหร" แล้วขึ้นข้อความหาไม่เจอ และตัวเลขแผงสรุปด้านบนยังอยู่ครบเท่าเดิม |
| 16:05 | B3.1 | "ทำปุ่มรับเรื่องใน RequestCard ให้กดแล้วอัปเดตสถานะเป็น in-progress ยังไง" | ให้เขียนฟังก์ชัน handleAcknowledge เรียก updateRequestStatus แล้วส่งผ่าน prop ไปที่การ์ด | เอาฟังก์ชันมาใส่ใน DashboardPage.jsx เพื่อจัดการอัปเดต state และ notice แล้วส่ง prop onAcknowledge ลงไปที่ RequestList เพื่อกระจายต่อไปยัง RequestCard จากนั้นเขียนปุ่ม "รับเรื่อง" ครอบเงื่อนไขให้โชว์เฉพาะการ์ดสถานะ pending พอทดลองกดปุ่ม REQ-101 พบว่า badge เปลี่ยนเป็น in-progress และตัวเลขในแผงสรุปคำนวณใหม่ทันที |
| 16:15 | B3.1 | "ใส่ปุ่มแล้วขึ้นหน้าขาว คอมไพล์ไม่ผ่าน แก้ตรงไหน" | ชี้จุดผิดใน RequestCard.jsx ว่ามีแท็ก </button> และวงเล็บปีกกาเกินคู่ | นำโค้ดมาไล่ดูตามที่ AI บอก เจอแท็กปิดแทรกอยู่ผิดตำแหน่งกับปีกกาปิดไม่ครบคู่ จึงทำการจัดโครงสร้าง JSX ใหม่ให้ปุ่มลบกับปุ่มรับเรื่องอยู่ในกลุ่มเดียวกัน พอเซฟไฟล์แล้วหน้าจอหายขาวกลับมาแสดงผลปกติ ปุ่มทำงานได้ตามเงื่อนไข |
| 16:30 | B3.2 | "กดรับเรื่องเปลี่ยนสถานะแล้ว พอกด F5 รีเฟรชหน้าเว็บ ข้อมูลจะยังอยู่ไหม" | อธิบายว่า updateRequestStatus เซฟลง LocalStorage อยู่แล้ว State จะดึงค่าใหม่มาแสดงอัตโนมัติ | เข้าใจหลักการว่าฟังก์ชันใน requestService.js มีการจัดการบันทึกลง LocalStorage ให้เรียบร้อยแล้ว จึงทดสอบโดยการกดปุ่มรับเรื่องที่ REQ-102 แล้วลองกด F5 รีเฟรชหน้าจอเบราว์เซอร์ดู ผลคือสถานะยังคงเป็น in-progress อยู่เหมือนเดิม ข้อมูลไม่เด้งกลับเป็นค่าเริ่มต้น แสดงว่าข้อมูล persist จริง |
| 16:38 | B3.3 | "เช็คค่า LocalStorage ที่ถูกบันทึกจริงใน DevTools ยังไง" | แนะนำวิธีเปิด DevTools แท็บ Application -> Local Storage เช็คคีย์ engse203-campus-requests-v1 | กด F12 เปิด DevTools เข้าไปส่องใน Application > Local Storage เพื่อยืนยันว่ามีคีย์ engse203-campus-requests-v1 อยู่จริง และลองแกะดู JSON ข้างในพบว่าข้อมูลอัปเดตตรงตามที่แสดงผลบนหน้าจอจริง ไม่ได้เป็นแค่ state ชั่วคราวบนหน่วยความจำ |
| 16:42 | B4.1 | "สร้าง Component StatusBadge ให้แปลงสถานะอังกฤษเป็นป้ายภาษาไทยต้องเขียนยังไง" | แนะนำให้ใช้ Object Mapping จับคู่สถานะ (pending, in-progress, completed) เข้ากับข้อความภาษาไทยและ CSS class | สร้างไฟล์ StatusBadge.jsx ตามโครงสร้างที่ได้ แล้วลองเขียน Object Mapping แปลงสถานะอังกฤษเป็นภาษาไทยตามโจทย์ |
| 16:48 | B4.2 | "ถ้าข้อมูลสถานะส่งมาเป็นค่าอื่นนอกจาก 3 สถานะนี้ หรือเป็น null/undefined ควรจัดการยังไงไม่ให้ Component พัง" | แนะนำให้ใส่ Fallback / Guard Clause เช็คกรณีค่าอื่น (Edge Case) ให้รีเทิร์นข้อความกลาง เช่น "ไม่ระบุ" | เพิ่มกรณี default หรือ Fallback ใน StatusBadge.jsx ให้แสดงป้าย "ไม่ระบุ" เอาไว้ด้วย เผื่อในอนาคต API หรือระบบฐานข้อมูลส่งสถานะใหม่ๆ หรือส่งค่าว่างมา โค้ดจะได้ไม่พังและ UI ยังแสดงผลได้สม่ำเสมอ |
| 16:55 | B4.3 | "เอา StatusBadge ไปใช้ใน RequestCard แทน span เดิมยังไง แล้วรัน commit ตามโจทย์" | ให้ import StatusBadge มาใช้ แล้วเปลี่ยนแท็ก <span> ดิบตรง TODO B4 ให้ส่ง prop status={request.status} เข้าไป | นำ StatusBadge ไปใส่ใน RequestCard.jsx แทนที่ <span> เดิม แล้วทดสอบบนเบราว์เซอร์พบว่าป้ายของการ์ดทุกใบกลายเป็นภาษาไทยถูกต้อง จากนั้นรันคำสั่ง git commit -m "B4.3: use badge in card" และ git push ดันงานขึ้น GitHub เรียบร้อย |

*(เพิ่มแถวได้ตามต้องการ — คัดลอกบรรทัด `| | | | | |` ไปวางเพิ่ม)*

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** ธีรนัย ไชยกันทะ 
